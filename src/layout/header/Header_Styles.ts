import styled from "styled-components";

 const Header = styled.header`
   background: rgba(32, 35, 42, 0.8);
   padding: 10px 0;
   position: fixed;
   top: 0;
   left: 0;
   right: 0;
   z-index: 99999;
 `

export const Styles = {
     Header,
}